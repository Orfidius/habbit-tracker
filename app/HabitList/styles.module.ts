import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  cardList: {
    padding: 0,
    paddingHorizontal: 20,
    rowGap: 2,
    paddingTop: 25,
    height: "100%",
    width: "100%",
    paddingBottom: 20,
  },
});
