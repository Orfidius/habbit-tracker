import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  cardList: {
    padding: 0,
    rowGap: 2,
    marginTop: 16,
    marginHorizontal: 20,
    display: "flex",
    flexDirection: "column",
    height: "100%",
    overflowY: "scroll",
  },
});
