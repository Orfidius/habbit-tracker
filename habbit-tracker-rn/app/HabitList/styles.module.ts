import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  cardList: {
    // listStyle: none,
    padding: 0,
    rowGap: 1,
    marginTop: 16,
    marginHorizontal: 12,
    display: "flex",
    flexDirection: "column",
    height: "100%",
    overflowY: "scroll",
  },
});
